---
description: Run the Tasks phase: break feature into atomic tasks with dependencies
argument-hint: "[feature-name]"
---
Load the l-spec skill and run the "tasks" command. 
If no arguments given, ask the user what feature/project they want to work on first.

