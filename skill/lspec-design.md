---
description: Run the Design phase: define architecture, components, and code reuse
argument-hint: "[feature-name]"
---
Load the l-spec skill and run the "design" command. 
If no arguments given, ask the user what feature/project they want to work on first.

