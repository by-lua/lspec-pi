---
description: Create task plan with dependencies and execution phases
argument-hint: "[feature-name]"
---
Load the l-spec skill and run the "tasks" command.
If no arguments given, ask the user what feature they want to plan tasks for.

After completing tasks, suggest the next step:
- Suggest `/lspec execute` to start implementing